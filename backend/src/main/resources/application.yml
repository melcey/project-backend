server:
  # The user microservice will run on port 8080
  port: 8080

spring:
  application:
    # The name of the Spring Boot application
    name: backend
  jpa:
    hibernate:
      ddl-auto: none # Hibernate will not check the current database schema at all
    show-sql: true # Logs the SQL statements generated by Hibernate
    properties:
      hibernate:
        "[format_sql]": true # Formats the SQL output for readability
  flyway:
    # Flyway is enabled on the project to run the migrations when the application starts
    enabled: true
    # Flyway sets a new baseline on each migration
    baseline-on-migrate: true
    # The location of the migrations
    locations: classpath:db/migration
    # The schema on which the migrations will be run
    default-schema: public
  mail:
    host: smtp.gmail.com
    port: 587
    username: group23cs308@gmail.com
    password: dndwzumcxpffsjld
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
        

app:
  jwt:
    # Secret key for signing JWT (should be at least 256 bits)
    secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
    # Token expiration time in milliseconds (24 hours)
    expiration: 86400000

upload:
  product_images:
    dir: /uploads/product-images